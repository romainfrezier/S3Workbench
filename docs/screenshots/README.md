# Product screenshots

Captured from the published S3Workbench 0.7.0 app on macOS with a local MinIO
connection and synthetic `studio-assets` data. No private endpoint, credentials,
or real customer objects are shown.

- `object-browser.jpg`: bucket navigation, object selection, metadata and custom headers.
- `recursive-search.jpg`: recursive results, full paths and persistent index status.

The website uses identical copies in `website/public/screenshots/`. The hero uses
a real application capture rather than an HTML reconstruction. Keep both copies
in sync when refreshing the captures.
