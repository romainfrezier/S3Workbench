# Native application QA — 2026-09-21

This is additional application-level evidence for [#15](https://github.com/romainfrezier/S3Workbench/issues/15),
[#17](https://github.com/romainfrezier/S3Workbench/issues/17) and
[#19](https://github.com/romainfrezier/S3Workbench/issues/19). It does not close their complete checklists.
No product code changed during this session.

## Exact scope

- macOS 27.0 beta, build 26A5425a, Apple Silicon.
- Existing local Debug app, About version **0.7.0 (19)**. This is not the published 0.7.0 binary.
- Executable SHA-256: `18c33609aa0b4ea5b9777f48f496a769b7a62c7d1be4abd07a960a0151087dcd`.
- The binary hash matches the September 18 follow-up evidence. Current source,
  tests, package files, packaging and scripts at `98d5cee` are identical to
  `develop` at `1bd442c9405c7e0fcf6d7b09ca5fe2f435e355f6`.
- Strict deep ad-hoc signature verification passed again on September 21.
- Existing disposable MinIO fixture bound to loopback port 19020, using the
  saved restricted QA connection and its synthetic `integration/` root.
- UI actions used native accessibility, keyboard and pointer input. No VoiceOver
  speech-output validation, stable-macOS validation or production-provider run.

The search fix is already integrated into `develop` through
[PR #45](https://github.com/romainfrezier/S3Workbench/pull/45), which replaced the
closed, unmerged PR #42. It has not been released on `main`.

## Checks completed in this session

| Area | Action | Observed result |
| --- | --- | --- |
| Settings | Open with Command-comma | Native Settings window opens. General, Connections, Advanced, Help and About are reachable. |
| Appearance | Select Dark, then Light | Settings visibly changes to each theme. Main-window theme propagation was not conclusively observed. |
| Preferences | Select Downloads and Keep Both for both collision policies; fully quit and reopen the same app | All three values and Light remain selected after restart. Collision menus expose Always Ask and Keep Both, without a persistent Replace option. |
| Draft protection | Change the QA connection name, then select Advanced | Save Changes, Discard Changes and Cancel are offered. Cancel retains the draft and disables Duplicate. Discard leaves the stored name unchanged. |
| Save and Keychain | Change the QA name again; choose Save Changes while both credential fields are blank | New name appears in the list. Reopening that connection and choosing Test Connection reports Connection succeeded, proving saved credentials remain usable. The original name is restored afterward. |
| Index settings | Open Advanced, request Clear Index on the QA connection, then cancel | Per-connection summaries appear; a confirmation explains the scope. Cancellation retains both summaries. The clear operation itself was not performed. |
| Help and About | Open both pages | Integrated guidance and website/GitHub/issues links are present. About reports 0.7.0 (19) and MIT License. External link destinations were not opened. |
| Single download | Select `alpha.txt`, press Command-S, choose a temporary directory, then Keep Both | The native destination panel is accepted and the downloaded file matches all 17 fixture bytes. |
| Multiple download | Select `alpha.txt` and `bravo.txt` with pointer and Shift-Down; choose Download from their context menu | Both remain selected after completion. Exact contents are written; existing `alpha.txt` is preserved and `alpha 2.txt` is created. |
| Search rebuild | Submit a query matching the nested fixture after the connection edit invalidated its index | UI reports 1,011 indexed objects and 1,005 matches. The rebuild uses two ListObjectsV2 calls. |
| Search draft | Replace the query with `alpha` without submitting | The 1,005 previous results remain visible; no new listing occurs. |
| Search submission | Submit `alpha`, select its result, invoke Command-S, then cancel the destination panel | One indexed match appears; selection and inspector survive cancellation. No listing occurs between rebuild completion and clearing the search. A HeadObject confirms that the trace is still receiving requests. |
| Clear search | Empty the field | Normal prefix browsing returns with one ListObjectsV2 call. |
| Navigation | Open `a/`, then use toolbar Back, Forward and Back | The expected root and child listings alternate; disabled states follow the navigation history. |

Downloaded contents were checked from disk, not inferred from a transfer label:

| File | Exact UTF-8 contents |
| --- | --- |
| `alpha.txt` | `alpha QA payload` followed by one newline |
| `alpha 2.txt` | `alpha QA payload` followed by one newline |
| `bravo.txt` | `bravo QA payload` followed by one newline |

The files remain in `/tmp/s3workbench-qa-20260921/downloads/` for local inspection.

## Network evidence

[Sanitized trace](2026-09-21-native-api-trace.jsonl) contains only UTC timestamps,
API names and HTTP statuses. No object keys, credentials, request headers,
signatures or response bodies are retained.

- 20:40:19: initial normal prefix listing.
- 20:41:50: single download; 20:43:16: two multi-file downloads.
- 20:43:41–42: exactly two listings rebuild the index.
- From completion of that rebuild until 20:45:57, draft editing, submission and
  the search-result destination panel add no listing. HeadObject at 20:44:08
  provides a positive trace control.
- 20:45:57: clearing the search lists the current prefix once.
- Later listings correspond to opening `a/` and Back/Forward/Back navigation.

## Limits and remaining work

- Closing Settings initially produced `noWindowsAvailable`/`timeoutReached`
  from UI automation. Command-comma restored access to Settings; a full app
  restart restored normal main-window control. No root cause or fix is claimed.
- Some transient menus were lost between inspection and activation. The
  multi-file context action succeeded when activated immediately from its
  freshly observed menu. A lingering Navigate menu required its native Cancel
  accessibility action. These automation boundaries are not product regressions.
- Command-left-bracket did not navigate with the search field focused in this
  run. Toolbar Back/Forward passed, but keyboard layout/input delivery versus
  application routing was not isolated. Keyboard coverage remains incomplete.
- Signed-link lifetime choices were inspected, but a changed lifetime was not
  successfully applied; persistence of that preference is not claimed.
- [#16](https://github.com/romainfrezier/S3Workbench/issues/16): no cross-application
  Finder/Desktop drag completed. Downloading and inspecting files in Finder do
  not prove file promises, deferred download, large-export memory, cancellation,
  incoming upload gestures or stale drag contexts.
- [#17](https://github.com/romainfrezier/S3Workbench/issues/17): add/duplicate/delete,
  save/delete failures, connection-scoped index clearing, main-window appearance,
  link preference persistence and VoiceOver remain outside this pass.
- [#15](https://github.com/romainfrezier/S3Workbench/issues/15): complete focus and
  multi-window shortcut coverage, other command entry points and VoiceOver remain
  outside this pass.
- [#19](https://github.com/romainfrezier/S3Workbench/issues/19): this run establishes
  rebuild/edit/submit/clear behavior. It does not repeat all cancellation, failed
  rebuild, restart, pagination/reveal and multi-window scenarios recorded in
  earlier local sessions; it is not a release-package or VoiceOver pass.

Unit tests, MinIO/RustFS integration suites and Release packaging were not rerun
for this documentation-only change. PR #45 records the earlier 103-test local
and MinIO runs, plus exact-head Swift CI, CodeQL Swift and Gitflow results; those
are historical evidence, not new test results from this session.

## State left behind

System appearance, Always Ask for all three transfer preferences and the
one-hour link lifetime were restored and read back in Settings. The QA
connection's original name was restored, and its invalidated index was rebuilt.
No connection or remote object was deleted. The existing MinIO container remains
running and healthy, with its volume retained. Only this session's trace container
was stopped. The original checkout's tracked diff is unchanged; all report work
is isolated on `chore/native-qa-20260921` from `origin/develop`.
